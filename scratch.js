require('dotenv').config({path: './.env'}); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function test() { const apps = await prisma.admissionApplication.findMany(); console.log('Apps count:', apps.length); const specific = await prisma.admissionApplication.findUnique({ where: { trackingToken: 'trk_2ad09e330ee04f7c87de40880d6034fb709521b28a6436' } }); console.log('Specific:', specific); } test().finally(() => prisma.$disconnect());
